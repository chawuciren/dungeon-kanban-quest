<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= config.app.name %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <!-- 自定义CSS -->
    <link href="/css/main.css" rel="stylesheet">
    
    <!-- 页面特定CSS -->
    <% if (typeof pageCSS !== 'undefined') { %>
        <% pageCSS.forEach(css => { %>
            <link href="<%= css %>" rel="stylesheet">
        <% }) %>
    <% } %>
</head>
<body>
    <!-- 导航栏 -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-gamepad me-2"></i>
                <%= config.app.name %>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">
                            <i class="fas fa-tachometer-alt me-1"></i>
                            仪表板
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/projects">
                            <i class="fas fa-project-diagram me-1"></i>
                            项目
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/tasks">
                            <i class="fas fa-tasks me-1"></i>
                            任务市场
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/leaderboard">
                            <i class="fas fa-trophy me-1"></i>
                            排行榜
                        </a>
                    </li>
                </ul>
                
                <ul class="navbar-nav">
                    <% if (user) { %>
                        <!-- 用户钱包 -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="walletDropdown" role="button" data-bs-toggle="dropdown">
                                <i class="fas fa-wallet me-1"></i>
                                钱包
                            </a>
                            <ul class="dropdown-menu">
                                <li><h6 class="dropdown-header">我的资产</h6></li>
                                <li>
                                    <span class="dropdown-item-text">
                                        <i class="fas fa-gem text-info me-1"></i>
                                        钻石: <span class="badge bg-info">0</span>
                                    </span>
                                </li>
                                <li>
                                    <span class="dropdown-item-text">
                                        <i class="fas fa-coins text-warning me-1"></i>
                                        金币: <span class="badge bg-warning">0</span>
                                    </span>
                                </li>
                                <li>
                                    <span class="dropdown-item-text">
                                        <i class="fas fa-circle text-secondary me-1"></i>
                                        银币: <span class="badge bg-secondary">0</span>
                                    </span>
                                </li>
                                <li>
                                    <span class="dropdown-item-text">
                                        <i class="fas fa-circle text-dark me-1"></i>
                                        铜币: <span class="badge bg-dark">0</span>
                                    </span>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/wallet">查看详情</a></li>
                            </ul>
                        </li>
                        
                        <!-- 用户菜单 -->
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                                <i class="fas fa-user me-1"></i>
                                <%= user.firstName %>
                                <span class="badge bg-<%= user.skillLevel === 'diamond' ? 'info' : user.skillLevel === 'gold' ? 'warning' : user.skillLevel === 'silver' ? 'secondary' : user.skillLevel === 'bronze' ? 'dark' : 'light' %> ms-1">
                                    <% if (user.skillLevel === 'diamond') { %>💎
                                    <% } else if (user.skillLevel === 'gold') { %>🥇
                                    <% } else if (user.skillLevel === 'silver') { %>🥈
                                    <% } else if (user.skillLevel === 'bronze') { %>🥉
                                    <% } else { %>🔰<% } %>
                                </span>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/profile">个人资料</a></li>
                                <li><a class="dropdown-item" href="/achievements">我的成就</a></li>
                                <li><a class="dropdown-item" href="/settings">设置</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/logout">退出登录</a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">登录</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">注册</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    
    <!-- 主要内容 -->
    <main class="container-fluid py-4">
        <!-- 消息提示 -->
        <div id="alerts-container"></div>
        
        <!-- 页面内容 -->
        <%- body %>
    </main>
    
    <!-- 页脚 -->
    <footer class="bg-light py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">&copy; 2024 <%= config.app.name %>. All rights reserved.</p>
                </div>
                <div class="col-md-6 text-end">
                    <small class="text-muted">
                        游戏化项目管理系统 v1.0.0
                    </small>
                </div>
            </div>
        </div>
    </footer>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    
    <!-- 自定义JS -->
    <script src="/js/main.js"></script>
    
    <!-- 页面特定JS -->
    <% if (typeof pageJS !== 'undefined') { %>
        <% pageJS.forEach(js => { %>
            <script src="<%= js %>"></script>
        <% }) %>
    <% } %>
</body>
</html>
