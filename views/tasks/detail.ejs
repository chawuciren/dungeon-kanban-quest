<%- include('../partials/header', { title: title }) %>

<%
// 统一的状态配置
const statusConfig = {
    'draft': { text: '草稿', class: 'secondary', icon: 'fas fa-edit' },
    'published': { text: '已发布', class: 'primary', icon: 'fas fa-bullhorn' },
    'in_progress': { text: '进行中', class: 'warning', icon: 'fas fa-play' },
    'review': { text: '待审核', class: 'info', icon: 'fas fa-search' },
    'completed': { text: '已完成', class: 'success', icon: 'fas fa-check' },
    'cancelled': { text: '已取消', class: 'danger', icon: 'fas fa-times' }
};
const currentStatus = statusConfig[task.status] || statusConfig['draft'];
%>

<div class="row">
        <!-- 主要内容 -->
        <div class="col-lg-8">
            <!-- 任务标题和基本信息 -->
            <div class="card mb-4">
                <div class="card-body">
                    <!-- 面包屑导航 -->
                    <nav aria-label="breadcrumb" class="mb-3">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/tasks">任务市场</a></li>
                            <% if (task.project) { %>
                                <li class="breadcrumb-item"><a href="/projects/<%= task.project.id %>"><%= task.project.name %></a></li>
                            <% } %>
                            <% if (task.parentTask) { %>
                                <li class="breadcrumb-item"><a href="/tasks/<%= task.parentTask.id %>"><%= task.parentTask.title %></a></li>
                            <% } %>
                            <li class="breadcrumb-item active"><%= task.title %></li>
                        </ol>
                    </nav>

                    <!-- 返回按钮 -->
                    <div class="mb-3">
                        <a href="/tasks" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-2"></i>返回任务列表
                        </a>
                    </div>

                    <!-- 任务标题 -->
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <h2 class="mb-0">
                            <span class="me-2">
                                <%= task.taskType === 'requirement' ? '📋' :
                                    task.taskType === 'epic' ? '🏰' :
                                    task.taskType === 'story' ? '📖' :
                                    task.taskType === 'dev_task' ? '⚔️' :
                                    task.taskType === 'design_task' ? '🎨' :
                                    task.taskType === 'test_task' ? '🏹' :
                                    task.taskType === 'devops_task' ? '⚙️' :
                                    task.taskType === 'bug' ? '🐛' : '📝' %>
                            </span>
                            <%= task.title %>
                        </h2>
                        <div class="task-actions">
                            <!-- 接单按钮 -->
                            <% if (canBid) { %>
                                <form action="/tasks/<%= task.id %>/bid" method="POST" style="display: inline;">
                                    <button type="submit" class="btn btn-primary">
                                        <i class="fas fa-hand-paper me-2"></i>接单
                                    </button>
                                </form>
                            <% } %>

                            <!-- 状态操作按钮 -->
                            <% if (user && (task.assigneeId === user.id || task.reviewerId === user.id || user.role === 'admin' || task.publisherId === user.id)) { %>
                                <% if (task.status === 'assigned' && task.assigneeId === user.id) { %>
                                    <form action="/tasks/<%= task.id %>/start" method="POST" style="display: inline;">
                                        <button type="submit" class="btn btn-success">
                                            <i class="fas fa-play me-2"></i>开始任务
                                        </button>
                                    </form>
                                <% } %>

                                <% if (['assigned', 'in_progress'].includes(task.status) && task.assigneeId === user.id) { %>
                                    <button type="button" class="btn btn-warning" onclick="showCompleteModal()">
                                        <i class="fas fa-check me-2"></i>完成任务
                                    </button>
                                <% } %>

                                <% if (task.status === 'review' && task.reviewerId === user.id) { %>
                                    <button type="button" class="btn btn-success" onclick="approveTask()">
                                        <i class="fas fa-check-circle me-2"></i>审核通过
                                    </button>
                                    <button type="button" class="btn btn-danger" onclick="rejectTask()">
                                        <i class="fas fa-times-circle me-2"></i>审核拒绝
                                    </button>
                                <% } %>
                            <% } %>

                            <!-- 管理按钮 -->
                            <%
                                // 检查当前用户是否有编辑权限
                                let canEdit = false;
                                if (user) {
                                    // 管理员始终有权限
                                    if (user.role === 'admin') {
                                        canEdit = true;
                                    }
                                    // 我创建的任务
                                    else if (task.publisherId === user.id) {
                                        canEdit = true;
                                    }
                                    // 我负责的任务
                                    else if (task.assigneeId === user.id) {
                                        canEdit = true;
                                    }
                                    // 我审核的任务
                                    else if (task.reviewerId === user.id) {
                                        canEdit = true;
                                    }
                                    // 项目所有者
                                    else if (task.project && task.project.ownerId === user.id) {
                                        canEdit = true;
                                    }
                                    // 项目负责人
                                    else if (task.project && task.project.leaderId === user.id) {
                                        canEdit = true;
                                    }
                                }
                            %>
                            <% if (canEdit) { %>
                                <a href="/tasks/<%= task.id %>/edit" class="btn btn-outline-primary">
                                    <i class="fas fa-edit me-2"></i>编辑任务
                                </a>
                            <% } %>

                            <% if (task.level < 3) { %>
                                <a href="/tasks/create?parent=<%= task.id %>" class="btn btn-outline-secondary">
                                    <i class="fas fa-plus me-2"></i>添加子任务
                                </a>
                            <% } %>
                        </div>
                    </div>

                    <!-- 任务元信息 -->
                    <div class="row g-3 mb-4">
                        <div class="col-auto">
                            <span class="badge bg-warning fs-6">
                                <%= '⭐'.repeat(task.starLevel) %>
                            </span>
                        </div>

                        <div class="col-auto">
                            <span class="badge bg-info fs-6">
                                <% if (task.urgencyLevel === 'urgent') { %>🔥 紧急
                                <% } else if (task.urgencyLevel === 'important') { %>⚡ 重要
                                <% } else if (task.urgencyLevel === 'normal') { %>📅 普通
                                <% } else if (task.urgencyLevel === 'delayed') { %>🕐 延后
                                <% } else if (task.urgencyLevel === 'frozen') { %>❄️ 冻结
                                <% } %>
                            </span>
                        </div>
                        <div class="col-auto">
                            <span class="badge bg-<%= currentStatus.class %> fs-6">
                                <i class="<%= currentStatus.icon %> me-1"></i>
                                <%= currentStatus.text %>
                            </span>
                        </div>
                    </div>

                    <!-- 任务描述 -->
                    <div class="mb-4">
                        <h5>任务描述</h5>
                        <div class="border rounded p-3 bg-light rich-text-content">
                            <%- task.description %>
                        </div>
                    </div>

                    <!-- 任务关系 -->
                    <% if (task.parentTask || (subtasks && subtasks.length > 0) || (siblingTasks && siblingTasks.length > 0)) { %>
                        <div class="mb-4">
                            <h5><i class="fas fa-sitemap me-2"></i>任务关系</h5>

                            <!-- 父任务信息 -->
                            <% if (task.parentTask) { %>
                                <div class="card mb-3">
                                    <div class="card-header bg-light">
                                        <h6 class="mb-0">
                                            <i class="fas fa-level-up-alt me-2"></i>父任务
                                        </h6>
                                    </div>
                                    <div class="card-body py-2">
                                        <div class="d-flex justify-content-between align-items-center">
                                            <div>
                                                <a href="/tasks/<%= task.parentTask.id %>" class="text-decoration-none">
                                                    <strong><%= task.parentTask.title %></strong>
                                                </a>
                                                <% if (task.parentTask.status) { %>
                                                    <%
                                                    const parentStatus = statusConfig[task.parentTask.status] || statusConfig['draft'];
                                                    %>
                                                    <span class="badge bg-<%= parentStatus.class %> ms-2">
                                                        <%= parentStatus.text %>
                                                    </span>
                                                <% } %>
                                            </div>
                                            <a href="/tasks/<%= task.parentTask.id %>" class="btn btn-outline-primary btn-sm">
                                                <i class="fas fa-external-link-alt me-1"></i>查看
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            <% } %>

                            <!-- 同级任务 -->
                            <% if (siblingTasks && siblingTasks.length > 0) { %>
                                <div class="card mb-3">
                                    <div class="card-header bg-light">
                                        <h6 class="mb-0">
                                            <i class="fas fa-equals me-2"></i>同级任务 (<%= siblingTasks.length %>)
                                        </h6>
                                    </div>
                                    <div class="card-body py-2">
                                        <div class="row g-2">
                                            <% siblingTasks.forEach(sibling => { %>
                                                <div class="col-12">
                                                    <div class="d-flex justify-content-between align-items-center py-1">
                                                        <div>
                                                            <a href="/tasks/<%= sibling.id %>" class="text-decoration-none">
                                                                <%= sibling.title %>
                                                            </a>
                                                            <%
                                                            const siblingStatus = statusConfig[sibling.status] || statusConfig['draft'];
                                                            %>
                                                            <span class="badge bg-<%= siblingStatus.class %> ms-2">
                                                                <%= siblingStatus.text %>
                                                            </span>
                                                        </div>
                                                        <small class="text-muted">
                                                            <%= '⭐'.repeat(sibling.starLevel) %>
                                                        </small>
                                                    </div>
                                                </div>
                                            <% }) %>
                                        </div>
                                    </div>
                                </div>
                            <% } %>

                            <!-- 子任务概览 -->
                            <% if (subtasks && subtasks.length > 0) { %>
                                <div class="card">
                                    <div class="card-header bg-light">
                                        <div class="d-flex justify-content-between align-items-center">
                                            <h6 class="mb-0">
                                                <i class="fas fa-level-down-alt me-2"></i>子任务 (<%= subtasks.length %>)
                                            </h6>
                                            <% if (task.level < 3) { %>
                                                <a href="/tasks/create?parent=<%= task.id %>" class="btn btn-outline-success btn-sm">
                                                    <i class="fas fa-plus me-1"></i>添加子任务
                                                </a>
                                            <% } %>
                                        </div>
                                    </div>
                                    <div class="card-body py-2">
                                        <!-- 子任务状态统计 -->
                                        <%
                                        const subtaskStats = {};
                                        subtasks.forEach(subtask => {
                                            const status = subtask.status || 'draft';
                                            subtaskStats[status] = (subtaskStats[status] || 0) + 1;
                                        });
                                        %>
                                        <div class="row g-2 mb-3">
                                            <% Object.keys(subtaskStats).forEach(status => { %>
                                                <%
                                                const statusInfo = statusConfig[status] || statusConfig['draft'];
                                                %>
                                                <div class="col-auto">
                                                    <span class="badge bg-<%= statusInfo.class %>">
                                                        <%= statusInfo.text %>: <%= subtaskStats[status] %>
                                                    </span>
                                                </div>
                                            <% }) %>
                                        </div>

                                        <!-- 子任务列表 -->
                                        <div class="list-group list-group-flush">
                                            <% subtasks.forEach(subtask => { %>
                                                <div class="list-group-item px-0 py-2">
                                                    <div class="d-flex justify-content-between align-items-center">
                                                        <div class="flex-grow-1">
                                                            <a href="/tasks/<%= subtask.id %>" class="text-decoration-none">
                                                                <strong><%= subtask.title %></strong>
                                                            </a>
                                                            <div class="mt-1">
                                                                <small class="text-muted">
                                                                    <%= '⭐'.repeat(subtask.starLevel) %>
                                                                    <% if (subtask.assignee) { %>
                                                                        | 负责人: <%= subtask.assignee.firstName %> <%= subtask.assignee.lastName %>
                                                                    <% } %>
                                                                    <% if (subtask.dueDate) { %>
                                                                        | 截止: <%= new Date(subtask.dueDate).toLocaleDateString('zh-CN') %>
                                                                    <% } %>
                                                                </small>
                                                            </div>
                                                        </div>
                                                        <div class="text-end">
                                                            <%
                                                            const subtaskStatus = statusConfig[subtask.status] || statusConfig['draft'];
                                                            %>
                                                            <span class="badge bg-<%= subtaskStatus.class %>">
                                                                <%= subtaskStatus.text %>
                                                            </span>
                                                        </div>
                                                    </div>
                                                </div>
                                            <% }) %>
                                        </div>
                                    </div>
                                </div>
                            <% } %>
                        </div>
                    <% } %>

                    <!-- 评论区块 -->
                    <div class="mb-4">
                        <h5 class="d-flex align-items-center justify-content-between">
                            <span>
                                <i class="fas fa-comments me-2"></i>
                                评论 (<%= totalComments || 0 %>)
                            </span>
                            <% if (commentsPagination && commentsPagination.totalPages > 1) { %>
                                <small class="text-muted">
                                    第 <%= commentsPagination.page %> 页，共 <%= commentsPagination.totalPages %> 页
                                </small>
                            <% } %>
                        </h5>

                        <!-- 新增评论编辑器 -->
                        <% if (user) { %>
                            <%- include('../partials/comment-editor', {
                                taskId: task.id,
                                editorId: 'mainCommentEditor',
                                placeholder: '写下您的评论...',
                                submitText: '发布评论',
                                projectMembers: projectMembers || [],
                                parentCommentId: null,
                                replyToUserId: null,
                                replyToUser: null
                            }) %>
                        <% } else { %>
                            <div class="alert alert-info">
                                <i class="fas fa-info-circle me-2"></i>
                                请先登录后再发表评论
                            </div>
                        <% } %>

                        <!-- 评论列表 -->
                        <%- include('../partials/comment-list', {
                            comments: comments || [],
                            user: user
                        }) %>

                        <!-- 评论分页 -->
                        <% if (commentsPagination && commentsPagination.totalPages > 1) { %>
                            <nav aria-label="评论分页" class="mt-4">
                                <ul class="pagination justify-content-center">
                                    <% if (commentsPagination.hasPrev) { %>
                                        <li class="page-item">
                                            <a class="page-link" href="?commentsPage=<%= commentsPagination.page - 1 %>#comments">
                                                <i class="fas fa-chevron-left"></i>
                                            </a>
                                        </li>
                                    <% } %>

                                    <% for (let i = Math.max(1, commentsPagination.page - 2); i <= Math.min(commentsPagination.totalPages, commentsPagination.page + 2); i++) { %>
                                        <li class="page-item <%= i === commentsPagination.page ? 'active' : '' %>">
                                            <a class="page-link" href="?commentsPage=<%= i %>#comments"><%= i %></a>
                                        </li>
                                    <% } %>

                                    <% if (commentsPagination.hasNext) { %>
                                        <li class="page-item">
                                            <a class="page-link" href="?commentsPage=<%= commentsPagination.page + 1 %>#comments">
                                                <i class="fas fa-chevron-right"></i>
                                            </a>
                                        </li>
                                    <% } %>
                                </ul>
                            </nav>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>

        <!-- 侧边栏 -->
        <div class="col-lg-4">
            <!-- 工时信息 -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-clock me-2"></i>工时信息
                    </h5>
                </div>
                <div class="card-body">
                    <!-- 本任务工时 -->
                    <div class="mb-3">
                        <h6 class="text-muted mb-2">
                            <i class="fas fa-user me-1"></i>本任务工时
                        </h6>
                        <div class="row text-center">
                            <div class="col-6">
                                <div class="border-end">
                                    <div class="fw-bold text-info"><%= hoursInfo.task.estimated.toFixed(1) %></div>
                                    <small class="text-muted">预估工时</small>
                                </div>
                            </div>
                            <div class="col-6">
                                <div class="fw-bold text-success"><%= hoursInfo.task.actual.toFixed(1) %></div>
                                <small class="text-muted">实际工时</small>
                            </div>
                        </div>
                    </div>

                    <!-- 子任务工时汇总 -->
                    <% if (hoursInfo.subtasks.count > 0) { %>
                        <hr class="my-3">

                        <div class="mb-3">
                            <h6 class="text-muted mb-2">
                                <i class="fas fa-level-down-alt me-1"></i>子任务工时汇总 (<%= hoursInfo.subtasks.count %>个)
                            </h6>
                            <div class="row text-center">
                                <div class="col-6">
                                    <div class="border-end">
                                        <div class="fw-bold text-info"><%= hoursInfo.subtasks.estimated.toFixed(1) %></div>
                                        <small class="text-muted">预估工时</small>
                                    </div>
                                </div>
                                <div class="col-6">
                                    <div class="fw-bold text-success"><%= hoursInfo.subtasks.actual.toFixed(1) %></div>
                                    <small class="text-muted">实际工时</small>
                                </div>
                            </div>
                        </div>

                        <hr class="my-3">

                        <!-- 总计工时 -->
                        <div class="mb-3">
                            <h6 class="text-primary mb-2">
                                <i class="fas fa-calculator me-1"></i>总计工时
                            </h6>
                            <div class="row text-center">
                                <div class="col-6">
                                    <div class="border-end">
                                        <div class="display-6 fw-bold text-primary"><%= hoursInfo.total.estimated.toFixed(1) %></div>
                                        <small class="text-muted">预估总计</small>
                                    </div>
                                </div>
                                <div class="col-6">
                                    <div class="display-6 fw-bold text-success"><%= hoursInfo.total.actual.toFixed(1) %></div>
                                    <small class="text-muted">实际总计</small>
                                </div>
                            </div>
                        </div>

                        <!-- 工时效率 -->
                        <% if (hoursInfo.total.actual > 0) { %>
                            <div class="text-center mb-3">
                                <div class="fw-bold <%= hoursInfo.total.efficiency >= 80 ? 'text-success' : hoursInfo.total.efficiency >= 60 ? 'text-warning' : 'text-danger' %> fs-4">
                                    <%= hoursInfo.total.efficiency.toFixed(1) %>%
                                </div>
                                <small class="text-muted">整体工时效率</small>
                            </div>
                        <% } %>
                    <% } else { %>
                        <!-- 无子任务时的工时效率 -->
                        <% if (hoursInfo.task.actual > 0) { %>
                            <hr class="my-3">
                            <div class="text-center mb-3">
                                <div class="fw-bold <%= hoursInfo.task.efficiency >= 80 ? 'text-success' : hoursInfo.task.efficiency >= 60 ? 'text-warning' : 'text-danger' %> fs-4">
                                    <%= hoursInfo.task.efficiency.toFixed(1) %>%
                                </div>
                                <small class="text-muted">工时效率</small>
                            </div>
                        <% } %>
                    <% } %>

                    <!-- 说明信息 -->
                    <div class="alert alert-light border-0 mb-0">
                        <small class="text-muted">
                            <i class="fas fa-info-circle me-1"></i>
                            工时效率 = 预估工时 ÷ 实际工时 × 100%<br>
                            <% if (hoursInfo.subtasks.count > 0) { %>
                                总计工时 = 本任务工时 + 所有子任务工时（递归统计）
                            <% } %>
                        </small>
                    </div>
                </div>
            </div>

            <!-- 任务信息 -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-info-circle me-2"></i>任务信息
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-12">
                            <strong>任务类型：</strong>
                            <span class="ms-2">
                                <span class="badge bg-<%=
                                    task.taskType === 'requirement' ? 'info' :
                                    task.taskType === 'epic' ? 'purple' :
                                    task.taskType === 'story' ? 'primary' :
                                    task.taskType === 'dev_task' ? 'primary' :
                                    task.taskType === 'design_task' ? 'success' :
                                    task.taskType === 'test_task' ? 'warning' :
                                    task.taskType === 'devops_task' ? 'dark' :
                                    task.taskType === 'bug' ? 'danger' : 'secondary'
                                %>">
                                    <%= task.taskType === 'requirement' ? '📋 需求' :
                                        task.taskType === 'epic' ? '🏰 史诗' :
                                        task.taskType === 'story' ? '📖 用户故事' :
                                        task.taskType === 'dev_task' ? '⚔️ 开发任务' :
                                        task.taskType === 'design_task' ? '🎨 设计任务' :
                                        task.taskType === 'test_task' ? '🏹 测试任务' :
                                        task.taskType === 'devops_task' ? '⚙️ 运维任务' :
                                        task.taskType === 'bug' ? '🐛 缺陷' : '📝 通用任务' %>
                                </span>
                            </span>
                        </div>
                        <div class="col-12">
                            <strong>发布者：</strong>
                            <span class="ms-2"><%= task.publisher.firstName %> <%= task.publisher.lastName %></span>
                        </div>
                        <% if (task.assignee) { %>
                            <div class="col-12">
                                <strong>负责人：</strong>
                                <span class="ms-2"><%= task.assignee.firstName %> <%= task.assignee.lastName %></span>
                            </div>
                        <% } %>
                        <% if (assistants && assistants.length > 0) { %>
                            <div class="col-12">
                                <strong>协助人员：</strong>
                                <span class="ms-2">
                                    <% assistants.forEach((assistant, index) => { %>
                                        <%= assistant.firstName %> <%= assistant.lastName %><% if (index < assistants.length - 1) { %>、<% } %>
                                    <% }); %>
                                </span>
                            </div>
                        <% } %>
                        <% if (task.reviewer) { %>
                            <div class="col-12">
                                <strong>审核人员：</strong>
                                <span class="ms-2"><%= task.reviewer.firstName %> <%= task.reviewer.lastName %></span>
                            </div>
                        <% } %>
                        <% if (task.estimatedHours) { %>
                            <div class="col-12">
                                <strong>预估工时：</strong>
                                <span class="ms-2"><%= task.estimatedHours %> 小时</span>
                            </div>
                        <% } %>
                        <% if (task.dueDate) { %>
                            <div class="col-12">
                                <strong>截止日期：</strong>
                                <span class="ms-2"><%= new Date(task.dueDate).toLocaleDateString('zh-CN') %></span>
                            </div>
                        <% } %>
                        <div class="col-12">
                            <strong>任务状态：</strong>
                            <span class="ms-2">
                                <span class="badge bg-<%= currentStatus.class %>">
                                    <i class="<%= currentStatus.icon %> me-1"></i>
                                    <%= currentStatus.text %>
                                </span>
                            </span>
                        </div>
                        <% if (task.startDate) { %>
                            <div class="col-12">
                                <strong>开始时间：</strong>
                                <span class="ms-2"><%= new Date(task.startDate).toLocaleDateString('zh-CN') %></span>
                            </div>
                        <% } %>
                        <% if (task.completedAt) { %>
                            <div class="col-12">
                                <strong>完成时间：</strong>
                                <span class="ms-2"><%= new Date(task.completedAt).toLocaleDateString('zh-CN') %></span>
                            </div>
                        <% } %>
                        <div class="col-12">
                            <strong>创建时间：</strong>
                            <span class="ms-2"><%= new Date(task.createdAt).toLocaleDateString('zh-CN') %></span>
                        </div>
                        <% if (task.level > 0) { %>
                            <div class="col-12">
                                <strong>任务层级：</strong>
                                <span class="ms-2">第 <%= task.level + 1 %> 级</span>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- 项目信息 -->
            <% if (task.project) { %>
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="fas fa-project-diagram me-2"></i>所属项目
                        </h5>
                    </div>
                    <div class="card-body">
                        <h6><%= task.project.name %></h6>
                        <p class="text-muted mb-2"><%= task.project.description || '暂无描述' %></p>
                        <a href="/projects/<%= task.project.id %>" class="btn btn-outline-primary btn-sm">
                            <i class="fas fa-external-link-alt me-1"></i>查看项目
                        </a>
                    </div>
                </div>
            <% } %>
    </div>
</div>

<!-- 完成任务模态框 -->
<div class="modal fade" id="completeModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">完成任务</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form action="/tasks/<%= task.id %>/complete" method="POST">
                <div class="modal-body">
                    <p>请确认任务已完成，并填写实际工时：</p>

                    <div class="mb-3">
                        <label for="actualHours" class="form-label">实际工时（小时）</label>
                        <input type="number" class="form-control" id="actualHours" name="actualHours"
                               min="0.5" step="0.5" placeholder="请输入实际工时">
                        <div class="form-text">
                            <% if (task.estimatedHours) { %>
                                预估工时：<%= task.estimatedHours %> 小时
                            <% } else { %>
                                填写实际工时有助于项目管理和奖励计算
                            <% } %>
                        </div>
                    </div>

                    <div class="alert alert-info">
                        <i class="fas fa-info-circle me-2"></i>
                        <% if (task.reviewerId) { %>
                            完成后任务将提交给审核人员进行审核。
                        <% } else { %>
                            完成后任务将直接标记为已完成。
                        <% } %>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
                    <button type="submit" class="btn btn-success">确认完成</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
// 全局变量
window.taskId = '<%= task.id %>';
window.projectMembersData = <%- JSON.stringify(projectMembers || []) %>;
window.currentUserId = '<%- typeof user !== 'undefined' && user ? user.id : 'null' %>';
window.currentUserRole = '<%- typeof user !== 'undefined' && user ? user.role : '' %>';

// 角色文本映射函数
function getRoleText(role) {
    const roleMap = {
        'product_manager': '产品经理',
        'developer': '开发者',
        'tester': '测试工程师',
        'ui_designer': 'UI设计师',
        'devops': '运维工程师',
        'client': '客户',
        'admin': '管理员'
    };
    return roleMap[role] || role;
}

$(document).ready(function() {
    // 页面初始化
    initCommentSystem();
});

// 初始化评论系统
function initCommentSystem() {
    // 初始化所有评论编辑器
    $('.comment-editor').each(function() {
        initCommentEditor($(this));
    });

    // 绑定回复按钮事件
    $(document).on('click', '.reply-btn', function() {
        const commentId = $(this).data('comment-id');
        const userId = $(this).data('user-id');
        const username = $(this).data('username');
        showReplyEditor(commentId, userId, username);
    });

    // 绑定编辑评论按钮事件
    $(document).on('click', '.edit-comment-btn', function() {
        const commentId = $(this).data('comment-id');
        editComment(commentId);
    });

    // 绑定删除评论按钮事件
    $(document).on('click', '.delete-comment-btn', function() {
        const commentId = $(this).data('comment-id');
        deleteComment(commentId);
    });

    // 绑定加载更多回复按钮事件
    $(document).on('click', '.load-more-replies-btn', function() {
        const commentId = $(this).data('comment-id');
        loadMoreReplies(commentId);
    });
}

// 初始化评论编辑器
function initCommentEditor($editor) {
    const $form = $editor.find('.comment-form');
    const $richEditor = $editor.find('.rich-editor-content');
    const $textarea = $editor.find('.comment-content');
    const $toolbar = $editor.find('.rich-editor-toolbar');
    const $imageUpload = $editor.find('.image-upload');
    const $mentionDropdown = $editor.find('.mention-dropdown');

    // 同步内容到隐藏的textarea
    function syncContent() {
        $textarea.val($richEditor.html());
    }

    // 监听内容变化
    $richEditor.on('input paste', function() {
        setTimeout(syncContent, 10);
    });

    // 工具栏按钮事件
    $toolbar.on('click', '.rich-editor-btn', function(e) {
        e.preventDefault();
        const command = $(this).data('command');

        if (command) {
            if (command === 'createLink') {
                const url = prompt('请输入链接地址:');
                if (url) {
                    document.execCommand(command, false, url);
                }
            } else {
                document.execCommand(command, false, null);
            }
            updateToolbarState($toolbar);
            syncContent();
        }
    });

    // 插入图片按钮
    $toolbar.on('click', '.insert-image-btn', function(e) {
        e.preventDefault();
        $imageUpload.click();
    });

    // @提醒按钮
    $toolbar.on('click', '.mention-btn', function(e) {
        e.preventDefault();
        insertMention($richEditor, $mentionDropdown);
    });

    // 图片上传处理
    $imageUpload.on('change', function(e) {
        const file = e.target.files[0];
        if (file) {
            console.log('图片文件选择:', file.name, file.size);
            uploadImage(file, $richEditor);
            $(this).val(''); // 清空文件输入
        }
    });

    // @用户选择
    $mentionDropdown.on('click', '.mention-item', function() {
        const username = $(this).data('username');
        const userId = $(this).data('user-id');
        insertUserMention($richEditor, username, userId);
        $mentionDropdown.hide();
    });

    // 表单提交
    $form.on('submit', function(e) {
        e.preventDefault();
        submitComment($form);
    });

    // 取消按钮
    $editor.on('click', '.cancel-comment, .cancel-reply', function() {
        if ($editor.attr('id') === 'mainCommentEditor') {
            // 主编辑器只清空内容
            $richEditor.html('');
            syncContent();
        } else {
            // 回复编辑器直接隐藏
            $editor.hide();
        }
    });

    // 更新工具栏状态
    $richEditor.on('mouseup keyup', function() {
        updateToolbarState($toolbar);
    });

    // 初始同步
    syncContent();
}

// 更新工具栏按钮状态
function updateToolbarState($toolbar) {
    $toolbar.find('.rich-editor-btn[data-command]').each(function() {
        const command = $(this).data('command');
        if (document.queryCommandState(command)) {
            $(this).addClass('active');
        } else {
            $(this).removeClass('active');
        }
    });
}

// 显示回复编辑器
function showReplyEditor(parentCommentId, replyToUserId, replyToUsername) {
    // 隐藏其他回复编辑器
    $('.reply-editor-container').hide().empty();

    // 获取项目成员数据（从页面中获取）
    const projectMembers = window.projectMembersData || [];

    // 生成项目成员选项HTML
    let memberOptionsHtml = '';
    projectMembers.forEach(member => {
        const roleText = getRoleText(member.role);
        memberOptionsHtml += `
            <button type="button" class="dropdown-item mention-item"
                    data-user-id="${member.id}"
                    data-username="${member.firstName}${member.lastName}">
                <div class="d-flex align-items-center">
                    <div class="flex-grow-1">
                        <div class="fw-medium">${member.firstName} ${member.lastName}</div>
                        <small class="text-muted">${roleText}</small>
                    </div>
                </div>
            </button>
        `;
    });

    // 创建回复编辑器HTML
    const editorHtml = `
        <div class="comment-editor mt-3" id="replyEditor_${parentCommentId}">
            <div class="card">
                <div class="card-body">
                    <form class="comment-form" data-task-id="${window.taskId}" data-parent-id="${parentCommentId}" data-reply-to="${replyToUserId}">
                        <div class="reply-indicator mb-2">
                            <small class="text-muted">
                                <i class="fas fa-reply me-1"></i>
                                回复 <strong>${replyToUsername}</strong>
                                <button type="button" class="btn btn-link btn-sm p-0 ms-2 cancel-reply">
                                    <i class="fas fa-times"></i>
                                </button>
                            </small>
                        </div>
                        <div class="rich-editor mb-3">
                            <div class="rich-editor-toolbar">
                                <button type="button" class="rich-editor-btn" data-command="bold" title="粗体">
                                    <i class="fas fa-bold"></i>
                                </button>
                                <button type="button" class="rich-editor-btn" data-command="italic" title="斜体">
                                    <i class="fas fa-italic"></i>
                                </button>
                                <button type="button" class="rich-editor-btn" data-command="underline" title="下划线">
                                    <i class="fas fa-underline"></i>
                                </button>
                                <span class="rich-editor-separator"></span>
                                <button type="button" class="rich-editor-btn insert-image-btn" title="插入图片">
                                    <i class="fas fa-image"></i>
                                </button>
                                <button type="button" class="rich-editor-btn mention-btn" title="@提醒">
                                    <i class="fas fa-at"></i>
                                </button>
                                <button type="button" class="rich-editor-btn" data-command="removeFormat" title="清除格式">
                                    <i class="fas fa-remove-format"></i>
                                </button>
                            </div>
                            <div class="rich-editor-content" contenteditable="true" placeholder="写下您的回复...">
                                @${replyToUsername}
                            </div>
                        </div>
                        <textarea class="form-control d-none comment-content" name="content" required></textarea>
                        <input type="file" class="image-upload" accept="image/*" style="display: none;">
                        <div class="mention-dropdown" style="display: none;">
                            <div class="dropdown-menu show">
                                ${memberOptionsHtml}
                            </div>
                        </div>
                        <div class="d-flex justify-content-end">
                            <button type="button" class="btn btn-outline-secondary btn-sm cancel-reply me-2">
                                <i class="fas fa-times me-1"></i>取消
                            </button>
                            <button type="submit" class="btn btn-primary btn-sm submit-comment">
                                <i class="fas fa-paper-plane me-1"></i>发布回复
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    `;

    // 插入到对应评论的回复容器中
    const $container = $(`.comment-item[data-comment-id="${parentCommentId}"] .reply-editor-container`);
    $container.html(editorHtml).show();

    // 初始化新创建的编辑器
    initCommentEditor($container.find('.comment-editor'));

    // 聚焦到编辑器
    $container.find('.rich-editor-content').focus();
}

// 提交评论
function submitComment($form) {
    const taskId = $form.data('task-id');
    const parentCommentId = $form.data('parent-id') || null;
    const replyToUserId = $form.data('reply-to') || null;
    const content = $form.find('.comment-content').val().trim();

    if (!content) {
        KanbanApp.showAlert('请输入评论内容', 'warning');
        return;
    }

    const $submitBtn = $form.find('.submit-comment');
    const originalText = $submitBtn.html();

    // 禁用提交按钮
    $submitBtn.prop('disabled', true).html('<i class="fas fa-spinner fa-spin me-1"></i>发布中...');

    $.ajax({
        url: `/tasks/${taskId}/comments`,
        method: 'POST',
        data: {
            content: content,
            parentCommentId: parentCommentId,
            replyToUserId: replyToUserId
        },
        success: function(response) {
            if (response.success) {
                KanbanApp.showAlert('评论发布成功', 'success');
                // 刷新页面以显示新评论
                setTimeout(() => {
                    window.location.reload();
                }, 1000);
            } else {
                KanbanApp.showAlert(response.message || '评论发布失败', 'danger');
            }
        },
        error: function(xhr) {
            const response = xhr.responseJSON;
            KanbanApp.showAlert(response?.message || '评论发布失败', 'danger');
        },
        complete: function() {
            // 恢复提交按钮
            $submitBtn.prop('disabled', false).html(originalText);
        }
    });
}

// 编辑评论
function editComment(commentId) {
    // 获取评论内容
    const $commentItem = $(`.comment-item[data-comment-id="${commentId}"], .comment-reply[data-comment-id="${commentId}"]`);
    const $contentDiv = $commentItem.find('.comment-content').first();
    const originalContent = $contentDiv.html();

    // 创建编辑表单
    const editFormHtml = `
        <div class="edit-comment-form">
            <div class="rich-editor mb-3">
                <div class="rich-editor-toolbar">
                    <button type="button" class="rich-editor-btn" data-command="bold" title="粗体">
                        <i class="fas fa-bold"></i>
                    </button>
                    <button type="button" class="rich-editor-btn" data-command="italic" title="斜体">
                        <i class="fas fa-italic"></i>
                    </button>
                    <button type="button" class="rich-editor-btn" data-command="underline" title="下划线">
                        <i class="fas fa-underline"></i>
                    </button>
                    <span class="rich-editor-separator"></span>
                    <button type="button" class="rich-editor-btn insert-image-btn" title="插入图片">
                        <i class="fas fa-image"></i>
                    </button>
                    <button type="button" class="rich-editor-btn" data-command="removeFormat" title="清除格式">
                        <i class="fas fa-remove-format"></i>
                    </button>
                </div>
                <div class="rich-editor-content" contenteditable="true">${originalContent}</div>
            </div>
            <textarea class="form-control d-none edit-content" required></textarea>
            <input type="file" class="image-upload" accept="image/*" style="display: none;">
            <div class="d-flex justify-content-end">
                <button type="button" class="btn btn-outline-secondary btn-sm cancel-edit me-2">
                    <i class="fas fa-times me-1"></i>取消
                </button>
                <button type="button" class="btn btn-primary btn-sm save-edit" data-comment-id="${commentId}">
                    <i class="fas fa-save me-1"></i>保存
                </button>
            </div>
        </div>
    `;

    // 替换内容区域
    $contentDiv.hide().after(editFormHtml);

    // 初始化编辑器
    const $editForm = $commentItem.find('.edit-comment-form');
    const $richEditor = $editForm.find('.rich-editor-content');
    const $textarea = $editForm.find('.edit-content');
    const $toolbar = $editForm.find('.rich-editor-toolbar');
    const $imageUpload = $editForm.find('.image-upload');

    // 同步内容
    function syncEditContent() {
        $textarea.val($richEditor.html());
    }

    $richEditor.on('input', syncEditContent);
    syncEditContent();

    // 工具栏按钮事件
    $toolbar.on('click', '.rich-editor-btn', function(e) {
        e.preventDefault();
        const command = $(this).data('command');

        if (command) {
            document.execCommand(command, false, null);
            syncEditContent();
        }
    });

    // 插入图片按钮
    $toolbar.on('click', '.insert-image-btn', function(e) {
        e.preventDefault();
        $imageUpload.click();
    });

    // 图片上传处理
    $imageUpload.on('change', function(e) {
        const file = e.target.files[0];
        if (file) {
            uploadImage(file, $richEditor);
            $(this).val(''); // 清空文件输入
        }
    });

    // 取消编辑
    $editForm.on('click', '.cancel-edit', function() {
        $editForm.remove();
        $contentDiv.show();
    });

    // 保存编辑
    $editForm.on('click', '.save-edit', function() {
        const content = $textarea.val().trim();
        if (!content) {
            KanbanApp.showAlert('请输入评论内容', 'warning');
            return;
        }

        const $saveBtn = $(this);
        const originalText = $saveBtn.html();
        $saveBtn.prop('disabled', true).html('<i class="fas fa-spinner fa-spin me-1"></i>保存中...');

        $.ajax({
            url: `/tasks/${window.taskId}/comments/${commentId}`,
            method: 'PUT',
            data: { content: content },
            success: function(response) {
                if (response.success) {
                    KanbanApp.showAlert('评论更新成功', 'success');
                    // 更新显示内容
                    $contentDiv.html(content);
                    $editForm.remove();
                    $contentDiv.show();

                    // 添加编辑标记
                    const $header = $commentItem.find('.comment-header .comment-time').first();
                    if (!$header.find('.fa-edit').length) {
                        $header.find('small').append(' <span class="ms-1" title="已编辑"><i class="fas fa-edit"></i></span>');
                    }
                } else {
                    KanbanApp.showAlert(response.message || '评论更新失败', 'danger');
                }
            },
            error: function(xhr) {
                const response = xhr.responseJSON;
                KanbanApp.showAlert(response?.message || '评论更新失败', 'danger');
            },
            complete: function() {
                $saveBtn.prop('disabled', false).html(originalText);
            }
        });
    });

    // 聚焦到编辑器
    $richEditor.focus();
}

// 删除评论
function deleteComment(commentId) {
    if (!confirm('确定要删除这条评论吗？此操作不可撤销。')) {
        return;
    }

    $.ajax({
        url: `/tasks/${window.taskId}/comments/${commentId}`,
        method: 'DELETE',
        success: function(response) {
            if (response.success) {
                KanbanApp.showAlert('评论删除成功', 'success');
                // 移除评论元素
                $(`.comment-item[data-comment-id="${commentId}"], .comment-reply[data-comment-id="${commentId}"]`).fadeOut(300, function() {
                    $(this).remove();
                    // 更新评论计数
                    updateCommentCount();
                });
            } else {
                KanbanApp.showAlert(response.message || '评论删除失败', 'danger');
            }
        },
        error: function(xhr) {
            const response = xhr.responseJSON;
            KanbanApp.showAlert(response?.message || '评论删除失败', 'danger');
        }
    });
}

// 加载更多回复
function loadMoreReplies(commentId) {
    const $btn = $(`.load-more-replies-btn[data-comment-id="${commentId}"]`);
    const originalText = $btn.html();
    const $repliesContainer = $(`.comment-replies[data-comment-id="${commentId}"]`);

    $btn.prop('disabled', true).html('<i class="fas fa-spinner fa-spin me-1"></i>加载中...');

    // 计算当前已显示的回复数量（排除"加载更多"按钮）
    const currentRepliesCount = $repliesContainer.find('.comment-reply').length;
    const limit = 10; // 每次加载10条

    // 使用offset参数精确控制数据获取
    $.ajax({
        url: `/tasks/${window.taskId}/comments/${commentId}/replies`,
        method: 'GET',
        data: {
            offset: currentRepliesCount, // 从当前已显示数量开始
            limit: limit
        },
        success: function(response) {
            if (response.success && response.data.length > 0) {
                // 渲染新的回复
                response.data.forEach(function(reply) {
                    const replyHtml = renderReplyHtml(reply);
                    $btn.parent().before(replyHtml);
                });

                // 检查是否还有更多回复
                const newTotalDisplayed = currentRepliesCount + response.data.length;
                if (newTotalDisplayed >= response.pagination.total) {
                    $btn.hide(); // 所有回复都已显示
                } else {
                    $btn.prop('disabled', false).html(originalText);
                }
            } else {
                $btn.hide(); // 没有更多回复
            }
        },
        error: function() {
            KanbanApp.showAlert('加载回复失败', 'danger');
            $btn.prop('disabled', false).html(originalText);
        }
    });
}

// 渲染回复HTML - 与原始模板保持一致
function renderReplyHtml(reply) {
    const userDisplayName = reply.user.firstName + ' ' + reply.user.lastName;

    // 创建主容器
    const $replyDiv = $('<div class="comment-reply"></div>').attr('data-comment-id', reply.id);

    // 创建连接线
    const $connector = $('<div class="reply-connector"></div>');
    $replyDiv.append($connector);

    // 创建回复内容容器
    const $content = $('<div class="reply-content"></div>');

    // 创建头部
    const $header = $('<div class="comment-header"></div>');

    // 创建作者信息
    const $author = $('<div class="comment-author"></div>');

    // 添加用户图标
    const $userIcon = $('<i class="fas fa-user-circle me-2 text-secondary"></i>');
    $author.append($userIcon);

    // 添加用户名
    const $authorName = $('<strong></strong>').text(userDisplayName);
    $author.append($authorName);

    // 添加回复目标信息（如果有）
    if (reply.replyToUser) {
        const $replyTo = $('<span class="reply-to ms-1"></span>');
        const $replyToStrong = $('<strong></strong>').text(reply.replyToUser.firstName + ' ' + reply.replyToUser.lastName);
        $replyTo.text('回复 ').append($replyToStrong);
        $author.append($replyTo);
    }

    // 添加角色徽章
    const $roleSpan = $('<span class="comment-role ms-2"></span>');
    const roleBadge = getRoleBadgeHtml(reply.user.role);
    if (roleBadge) {
        $roleSpan.html(roleBadge);
        $author.append($roleSpan);
    }

    // 创建时间信息
    const $time = $('<div class="comment-time"></div>');
    const $timeSmall = $('<small class="text-muted"></small>');
    $timeSmall.text(formatDateTime(reply.createdAt));

    // 添加编辑标记（如果有）
    if (reply.isEdited) {
        const $editedSpan = $('<span class="ms-1"></span>').attr('title', '已编辑');
        const $editIcon = $('<i class="fas fa-edit"></i>');
        $editedSpan.append($editIcon);
        $timeSmall.append($editedSpan);
    }

    $time.append($timeSmall);

    $header.append($author, $time);
    $content.append($header);

    // 创建评论内容
    const $commentContent = $('<div class="comment-content rich-text-content"></div>').html(reply.content);
    $content.append($commentContent);

    // 创建操作区域
    const $actions = $('<div class="comment-actions"></div>');

    // 统计信息
    const $stats = $('<div class="comment-stats"></div>');
    if (reply.likesCount > 0) {
        const $likes = $('<span class="likes-count"></span>')
            .html(`<i class="fas fa-thumbs-up text-primary me-1"></i>${reply.likesCount}`);
        $stats.append($likes);
    }

    // 按钮组
    const $buttons = $('<div class="comment-buttons"></div>');

    // 回复按钮
    const $replyBtn = $('<button type="button" class="btn btn-link btn-sm reply-btn"></button>')
        .attr('data-comment-id', reply.parentCommentId)
        .attr('data-user-id', reply.user.id)
        .attr('data-username', reply.user.firstName + reply.user.lastName)
        .html('<i class="fas fa-reply me-1"></i>回复');
    $buttons.append($replyBtn);

    // 编辑按钮（仅作者可见）
    if (reply.userId === window.currentUserId) {
        const $editBtn = $('<button type="button" class="btn btn-link btn-sm edit-comment-btn"></button>')
            .attr('data-comment-id', reply.id)
            .html('<i class="fas fa-edit me-1"></i>编辑');
        $buttons.append($editBtn);
    }

    // 删除按钮（作者和管理员可见）
    if (reply.userId === window.currentUserId || window.currentUserRole === 'admin') {
        const $deleteBtn = $('<button type="button" class="btn btn-link btn-sm text-danger delete-comment-btn"></button>')
            .attr('data-comment-id', reply.id)
            .html('<i class="fas fa-trash me-1"></i>删除');
        $buttons.append($deleteBtn);
    }

    $actions.append($stats, $buttons);
    $content.append($actions);
    $replyDiv.append($content);

    return $replyDiv[0].outerHTML;
}

// 获取角色信息
function getRoleInfo(role) {
    const roleMap = {
        'admin': { name: '管理员', class: 'bg-danger' },
        'client': { name: '客户', class: 'bg-purple' },
        'product_manager': { name: '产品经理', class: 'bg-primary' },
        'developer': { name: '开发', class: 'bg-success' },
        'tester': { name: '测试', class: 'bg-warning' },
        'designer': { name: '设计师', class: 'bg-info' },
        'ops': { name: '运维', class: 'bg-dark' }
    };
    return roleMap[role] || { name: '成员', class: 'bg-secondary' };
}

// 获取角色徽章HTML - 与原始模板保持一致
function getRoleBadgeHtml(role) {
    switch (role) {
        case 'product_manager':
            return '<span class="badge bg-info">产品经理</span>';
        case 'developer':
            return '<span class="badge bg-primary">开发者</span>';
        case 'tester':
            return '<span class="badge bg-warning">测试工程师</span>';
        case 'ui_designer':
            return '<span class="badge bg-success">UI设计师</span>';
        case 'devops':
            return '<span class="badge bg-dark">运维工程师</span>';
        case 'client':
            return '<span class="badge bg-purple">客户</span>';
        case 'admin':
            return '<span class="badge bg-danger">管理员</span>';
        default:
            return null; // 不显示徽章
    }
}

// 格式化日期时间
function formatDateTime(dateString) {
    const date = new Date(dateString);
    const now = new Date();
    const diff = now - date;

    // 小于1分钟
    if (diff < 60000) {
        return '刚刚';
    }

    // 小于1小时
    if (diff < 3600000) {
        const minutes = Math.floor(diff / 60000);
        return `${minutes}分钟前`;
    }

    // 小于24小时
    if (diff < 86400000) {
        const hours = Math.floor(diff / 3600000);
        return `${hours}小时前`;
    }

    // 小于7天
    if (diff < 604800000) {
        const days = Math.floor(diff / 86400000);
        return `${days}天前`;
    }

    // 超过7天显示具体日期
    return date.toLocaleDateString('zh-CN', {
        year: 'numeric',
        month: 'short',
        day: 'numeric',
        hour: '2-digit',
        minute: '2-digit'
    });
}

// 更新评论计数
function updateCommentCount() {
    const totalComments = $('.comment-item, .comment-reply').length;
    $('.comments-section h5 span').html(`<i class="fas fa-comments me-2"></i>评论 (${totalComments})`);
}

// 上传图片
function uploadImage(file, $richEditor) {
    console.log('开始上传图片:', file.name, '编辑器:', $richEditor.length);
    const formData = new FormData();
    formData.append('taskImage', file);

    $.ajax({
        url: '/tasks/upload-image',
        method: 'POST',
        data: formData,
        processData: false,
        contentType: false,
        success: function(response) {
            console.log('图片上传响应:', response);
            if (response.success) {
                // 插入图片到编辑器
                const img = document.createElement('img');
                img.src = response.location;
                img.style.maxWidth = '100%';
                img.style.height = 'auto';

                // 获取当前选择或光标位置
                const selection = window.getSelection();
                if (selection.rangeCount > 0) {
                    const range = selection.getRangeAt(0);
                    range.insertNode(img);
                    range.collapse(false);
                } else {
                    $richEditor[0].appendChild(img);
                }

                // 重要：同步内容到隐藏的textarea
                const $editor = $richEditor.closest('.comment-editor');
                const $editForm = $richEditor.closest('.edit-comment-form');

                if ($editor.length > 0) {
                    // 普通评论编辑器
                    const $textarea = $editor.find('.comment-content');
                    $textarea.val($richEditor.html());
                } else if ($editForm.length > 0) {
                    // 编辑评论表单
                    const $textarea = $editForm.find('.edit-content');
                    $textarea.val($richEditor.html());
                }

                KanbanApp.showAlert('图片上传成功', 'success');
            } else {
                KanbanApp.showAlert('图片上传失败: ' + (response.error || '未知错误'), 'danger');
            }
        },
        error: function() {
            KanbanApp.showAlert('图片上传失败', 'danger');
        }
    });
}

// 插入@提醒
function insertMention($richEditor, $mentionDropdown) {
    // 简化实现：直接显示用户列表
    const rect = $richEditor[0].getBoundingClientRect();
    $mentionDropdown.css({
        position: 'absolute',
        top: rect.bottom + 'px',
        left: rect.left + 'px'
    }).show();
}

// 插入用户提醒
function insertUserMention($richEditor, username, userId) {
    const mention = `@${username} `;

    // 获取当前选择或光标位置
    const selection = window.getSelection();
    if (selection.rangeCount > 0) {
        const range = selection.getRangeAt(0);
        const textNode = document.createTextNode(mention);
        range.insertNode(textNode);
        range.setStartAfter(textNode);
        range.collapse(true);
        selection.removeAllRanges();
        selection.addRange(range);
    } else {
        $richEditor.append(mention);
    }

    $richEditor.focus();
}

// 显示完成任务模态框
function showCompleteModal() {
    $('#completeModal').modal('show');
}

// 审核通过
function approveTask() {
    if (confirm('确定要审核通过此任务吗？')) {
        updateTaskStatus('completed', '审核通过，任务已完成！');
    }
}

// 审核拒绝
function rejectTask() {
    if (confirm('确定要拒绝此任务吗？任务将返回给负责人重新处理。')) {
        updateTaskStatus('in_progress', '审核拒绝，任务已返回给负责人');
    }
}

// 更新任务状态
function updateTaskStatus(newStatus, message) {
    const taskId = window.taskId;

    $.ajax({
        url: `/tasks/${taskId}/status`,
        method: 'POST',
        data: { status: newStatus },
        success: function(response) {
            if (response.success) {
                KanbanApp.showAlert(message || response.message, 'success');
                // 刷新页面以显示最新状态
                setTimeout(() => {
                    window.location.reload();
                }, 1500);
            } else {
                KanbanApp.showAlert(response.message, 'danger');
            }
        },
        error: function(xhr) {
            const response = xhr.responseJSON;
            KanbanApp.showAlert(response?.message || '操作失败', 'danger');
        }
    });
}
</script>

<style>
/* 富文本内容样式 */
.rich-text-content {
    line-height: 1.6;
}

.rich-text-content img {
    max-width: 100%;
    height: auto;
    border-radius: 4px;
    margin: 10px 0;
}

.rich-text-content p {
    margin-bottom: 1rem;
}

.rich-text-content ul, .rich-text-content ol {
    margin-bottom: 1rem;
    padding-left: 2rem;
}

.rich-text-content blockquote {
    border-left: 4px solid #007bff;
    padding-left: 1rem;
    margin: 1rem 0;
    font-style: italic;
    color: #6c757d;
}

.rich-text-content code {
    background-color: #f8f9fa;
    padding: 2px 4px;
    border-radius: 3px;
    font-family: 'Courier New', monospace;
}

.rich-text-content pre {
    background-color: #f8f9fa;
    padding: 1rem;
    border-radius: 4px;
    overflow-x: auto;
}

.rich-text-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 1rem 0;
}

.rich-text-content table th,
.rich-text-content table td {
    border: 1px solid #dee2e6;
    padding: 8px 12px;
    text-align: left;
}

.rich-text-content table th {
    background-color: #f8f9fa;
    font-weight: 600;
}

/* 评论系统样式 */
.comments-section {
    margin-top: 2rem;
}

.comment-item {
    margin-bottom: 1.5rem;
}

.comment-main {
    background-color: #fff;
    border: 1px solid #e1e5e9;
    border-radius: 0.5rem;
    padding: 1rem;
}

.comment-header {
    display: flex;
    justify-content: between;
    align-items: center;
    margin-bottom: 0.75rem;
}

.comment-author {
    display: flex;
    align-items: center;
    flex-grow: 1;
}

.comment-role .badge {
    font-size: 0.7rem;
}

.comment-time {
    color: #6c757d;
    font-size: 0.875rem;
}

.comment-content {
    margin-bottom: 0.75rem;
    color: #333;
}

.comment-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.comment-buttons .btn {
    padding: 0.25rem 0.5rem;
    font-size: 0.875rem;
    color: #6c757d;
    text-decoration: none;
}

.comment-buttons .btn:hover {
    color: #007bff;
    text-decoration: none;
}

.comment-buttons .btn.text-danger:hover {
    color: #dc3545 !important;
}

/* 回复样式 */
.comment-replies {
    margin-top: 1rem;
    margin-left: 40px;
    border-left: 2px solid #e1e5e9;
    padding-left: 20px;
    position: relative;
}

.comment-reply {
    margin-bottom: 1rem;
    position: relative;
}

.reply-connector {
    position: absolute;
    left: -22px;
    top: 1rem;
    width: 20px;
    height: 2px;
    background-color: #e1e5e9;
}

.reply-content {
    background-color: #f8f9fa;
    border: 1px solid #e1e5e9;
    border-radius: 0.375rem;
    padding: 0.75rem;
}

.reply-to {
    color: #007bff;
    font-size: 0.875rem;
}

.load-more-replies {
    margin-top: 0.5rem;
    text-align: center;
}

.load-more-replies .btn {
    color: #6c757d;
    font-size: 0.875rem;
}

/* 无评论状态 */
.no-comments {
    color: #6c757d;
    padding: 3rem 1rem;
}

/* 响应式设计 */
@media (max-width: 768px) {
    .comment-replies {
        margin-left: 20px;
        padding-left: 15px;
    }

    .comment-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }

    .comment-actions {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }
}

@media (max-width: 576px) {
    .comment-replies {
        margin-left: 15px;
        padding-left: 10px;
    }

    .comment-main,
    .reply-content {
        padding: 0.75rem;
    }
}

/* 紫色徽章样式 */
.badge.bg-purple {
    background-color: #6f42c1 !important;
}
</style>

<%- include('../partials/footer') %>
